# skills

This repository stores multiple Codex/OpenClaw skills.

Current skills:

- `add-feishu-bot-agent-skill`

Each skill lives in its own subdirectory and keeps its own `SKILL.md`, scripts, and supporting files.
